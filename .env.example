# Copy to .env.local for local dev, and add both to your host's
# Environment Variables (Production, Preview and Development) before deploying.
#
# Both are safe to expose to the browser. Row Level Security is what protects
# the data: the anon role holds INSERT on `waitlist` and nothing else.
#
# Find them in Supabase: Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://ckhgfcqpqlrucovefyjx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_LQGaOo3yrbDxm73oiNUWuw_BTNOr47b
